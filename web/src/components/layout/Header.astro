---
/**
 * Header.astro (v2)
 * Fixed dark charcoal header with desktop nav (dropdowns for Services + Areas),
 * dark mobile drawer, and phone CTA. Red hairline signal at the very top.
 */

import config from '@/site.config';
import { getBusiness, getServices, getServiceAreas } from '../../lib/data';
import { homeUrl, contactUrl, serviceUrl, areaUrl, servicesUrl, areasUrl } from '../../lib/urls';

const business     = await getBusiness();
const services     = await getServices();
const areas        = config.features.serviceAreas ? await getServiceAreas() : [];

const currentPath = Astro.url.pathname;

// Only primary/secondary areas in the dropdown (cap at 10 to keep it manageable)
const dropdownAreas = areas
  .filter(a => a.priority === 'primary' || a.priority === 'secondary')
  .slice(0, 10);

const navLinks = [
  { label: 'Services',      href: servicesUrl(),  hasDropdown: true,  dropdownId: 'dd-services' },
  ...(config.features.serviceAreas ? [{ label: 'Service Areas', href: areasUrl(), hasDropdown: true, dropdownId: 'dd-areas' }] : []),
  ...(config.features.projects     ? [{ label: 'Projects',      href: '/projects/', hasDropdown: false, dropdownId: '' }] : []),
  ...(config.features.blog         ? [{ label: 'Blog',          href: '/blog/',     hasDropdown: false, dropdownId: '' }] : []),
  { label: 'About',   href: '/about/',   hasDropdown: false, dropdownId: '' },
  { label: 'Careers', href: '/careers/', hasDropdown: false, dropdownId: '' },
  { label: 'Contact', href: '/contact/', hasDropdown: false, dropdownId: '' },
];
---

<header class="site-header" id="site-header">
  <div class="header-accent-line"></div>
  <div class="container-wide">
    <nav class="header-nav" aria-label="Main navigation">

      <!-- Logo -->
      <a href={homeUrl()} class="header-logo" aria-label={`${business.name}, home`}>
        <img
          src="/JP%20Construction%20Light%20Trimmed.svg"
          alt={business.name}
          class="logo-img"
          width="206"
          height="40"
          loading="eager"
        />
      </a>

      <!-- Desktop Nav -->
      <ul class="desktop-nav" role="list">
        {navLinks.map(link => (
          <li class:list={['nav-item', { 'has-dropdown': link.hasDropdown }]}>
            {link.hasDropdown ? (
              <button
                class:list={['nav-link nav-link--btn', { 'nav-link--active': currentPath.startsWith(link.href) }]}
                aria-expanded="false"
                aria-controls={link.dropdownId}
                aria-haspopup="true"
                data-dropdown-trigger={link.dropdownId}
              >
                {link.label}
                <svg class="nav-chevron" width="11" height="11" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" aria-hidden="true">
                  <polyline points="6 9 12 15 18 9"/>
                </svg>
              </button>
            ) : (
              <a
                href={link.href}
                class:list={['nav-link', { 'nav-link--active': currentPath.startsWith(link.href) }]}
              >
                {link.label}
              </a>
            )}

            {/* Services Dropdown */}
            {link.dropdownId === 'dd-services' && (
              <div class="nav-dropdown" id="dd-services" role="region" aria-label="Services menu">
                <div class="dropdown-inner">
                  <div class="dropdown-header">
                    <span class="dropdown-label">Services</span>
                    <a href={servicesUrl()} class="dropdown-view-all">View All</a>
                  </div>
                  <ul class="dropdown-list" role="list">
                    {services.map((service, i) => (
                      <li>
                        <a href={serviceUrl(service.slug)} class="dropdown-item">
                          <span class="dropdown-item-num">{String(i + 1).padStart(2, '0')}</span>
                          <span class="dropdown-item-body">
                            <span class="dropdown-item-name">{service.name}</span>
                            <span class="dropdown-item-desc">{service.shortDescription}</span>
                          </span>
                        </a>
                      </li>
                    ))}
                  </ul>
                </div>
              </div>
            )}

            {/* Areas Dropdown */}
            {link.dropdownId === 'dd-areas' && dropdownAreas.length > 0 && (
              <div class="nav-dropdown nav-dropdown--areas" id="dd-areas" role="region" aria-label="Service areas menu">
                <div class="dropdown-inner">
                  <div class="dropdown-header">
                    <span class="dropdown-label">Service Areas</span>
                    <a href={areasUrl()} class="dropdown-view-all">View All</a>
                  </div>
                  <ul class="dropdown-list dropdown-list--grid" role="list">
                    {dropdownAreas.map(area => (
                      <li>
                        <a href={areaUrl(area.slug)} class="dropdown-item dropdown-item--compact">
                          <span class="dropdown-item-name">{area.name}</span>
                        </a>
                      </li>
                    ))}
                  </ul>
                </div>
              </div>
            )}
          </li>
        ))}
      </ul>

      <!-- Desktop CTA -->
      <div class="header-cta">
        <a href={business.phoneHref} class="btn-call">
          <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" aria-hidden="true">
            <path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07A19.5 19.5 0 0 1 4.69 12 19.79 19.79 0 0 1 1.77 3.44 2 2 0 0 1 3.74 1.26h3a2 2 0 0 1 2 1.72c.127.96.361 1.903.7 2.81a2 2 0 0 1-.45 2.11L7.91 9a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45c.907.339 1.85.573 2.81.7A2 2 0 0 1 22 16.92z"/>
          </svg>
          {business.phone}
        </a>
      </div>

      <!-- Mobile Menu Toggle -->
      <button
        class="mobile-menu-toggle"
        id="mobile-menu-toggle"
        aria-expanded="false"
        aria-controls="mobile-drawer"
        aria-label="Open navigation menu"
      >
        <span class="toggle-bar"></span>
        <span class="toggle-bar"></span>
        <span class="toggle-bar"></span>
      </button>
    </nav>
  </div>
</header>

<!-- Mobile Drawer -->
<div class="mobile-drawer" id="mobile-drawer" aria-hidden="true" role="dialog" aria-label="Navigation menu">
  <div class="mobile-drawer-inner">

    <div class="mobile-drawer-header">
      <img src="/JP%20Construction%20Light%20Trimmed.svg" alt={business.name} class="drawer-logo" width="160" height="32" loading="lazy" />
      <button
        class="mobile-drawer-close"
        id="mobile-drawer-close"
        aria-label="Close navigation menu"
      >
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5">
          <line x1="18" y1="6" x2="6" y2="18"/>
          <line x1="6" y1="6" x2="18" y2="18"/>
        </svg>
      </button>
    </div>

    <nav aria-label="Mobile navigation">
      <ul role="list" class="mobile-nav-list">

        <!-- Services: expandable section -->
        <li class="mobile-nav-section">
          <div class="mobile-nav-row">
            <a href={servicesUrl()} class:list={['mobile-nav-link mobile-nav-link--parent', { 'mobile-nav-link--active': currentPath.startsWith(servicesUrl()) }]}>
              Services
            </a>
            {services.length > 0 && (
              <button
                class="mobile-sub-toggle"
                aria-expanded="false"
                aria-controls="msub-services"
                aria-label="Show services"
                data-sub-toggle
              >
                <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true">
                  <polyline points="6 9 12 15 18 9"/>
                </svg>
              </button>
            )}
          </div>
          {services.length > 0 && (
            <div class="mobile-sub-wrap" id="msub-services">
              <ul class="mobile-sub-list" role="list">
                {services.map(service => (
                  <li>
                    <a href={serviceUrl(service.slug)} class="mobile-sub-link">{service.name}</a>
                  </li>
                ))}
              </ul>
            </div>
          )}
        </li>

        <!-- Service Areas: expandable section -->
        {config.features.serviceAreas && dropdownAreas.length > 0 && (
          <li class="mobile-nav-section">
            <div class="mobile-nav-row">
              <a href={areasUrl()} class:list={['mobile-nav-link mobile-nav-link--parent', { 'mobile-nav-link--active': currentPath.startsWith(areasUrl()) }]}>
                Service Areas
              </a>
              <button
                class="mobile-sub-toggle"
                aria-expanded="false"
                aria-controls="msub-areas"
                aria-label="Show service areas"
                data-sub-toggle
              >
                <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true">
                  <polyline points="6 9 12 15 18 9"/>
                </svg>
              </button>
            </div>
            <div class="mobile-sub-wrap" id="msub-areas">
              <ul class="mobile-sub-list" role="list">
                {dropdownAreas.map(area => (
                  <li>
                    <a href={areaUrl(area.slug)} class="mobile-sub-link">{area.name}</a>
                  </li>
                ))}
              </ul>
            </div>
          </li>
        )}

        {config.features.projects && (
          <li>
            <a href="/projects/" class:list={['mobile-nav-link', { 'mobile-nav-link--active': currentPath.startsWith('/projects/') }]}>
              Projects
              <svg class="mobile-nav-arrow" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true">
                <polyline points="9 18 15 12 9 6"/>
              </svg>
            </a>
          </li>
        )}

        {config.features.blog && (
          <li>
            <a href="/blog/" class:list={['mobile-nav-link', { 'mobile-nav-link--active': currentPath.startsWith('/blog/') }]}>
              Blog
              <svg class="mobile-nav-arrow" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true">
                <polyline points="9 18 15 12 9 6"/>
              </svg>
            </a>
          </li>
        )}

        <li>
          <a href="/about/" class:list={['mobile-nav-link', { 'mobile-nav-link--active': currentPath === '/about/' }]}>
            About
            <svg class="mobile-nav-arrow" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true">
              <polyline points="9 18 15 12 9 6"/>
            </svg>
          </a>
        </li>

        <li>
          <a href="/careers/" class:list={['mobile-nav-link', { 'mobile-nav-link--active': currentPath.startsWith('/careers/') }]}>
            Careers
            <svg class="mobile-nav-arrow" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true">
              <polyline points="9 18 15 12 9 6"/>
            </svg>
          </a>
        </li>

        <li>
          <a href="/contact/" class:list={['mobile-nav-link', { 'mobile-nav-link--active': currentPath === '/contact/' }]}>
            Contact
            <svg class="mobile-nav-arrow" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true">
              <polyline points="9 18 15 12 9 6"/>
            </svg>
          </a>
        </li>
      </ul>
    </nav>

    <div class="mobile-drawer-cta">
      <a href={business.phoneHref} class="drawer-btn-call">
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" aria-hidden="true">
          <path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07A19.5 19.5 0 0 1 4.69 12 19.79 19.79 0 0 1 1.77 3.44 2 2 0 0 1 3.74 1.26h3a2 2 0 0 1 2 1.72c.127.96.361 1.903.7 2.81a2 2 0 0 1-.45 2.11L7.91 9a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45c.907.339 1.85.573 2.81.7A2 2 0 0 1 22 16.92z"/>
        </svg>
        {business.phone}
      </a>
      <a href={contactUrl()} class="btn btn-ghost btn-block">
        Get an Estimate
      </a>
    </div>
  </div>
</div>

<!-- Overlay -->
<div class="mobile-overlay" id="mobile-overlay" aria-hidden="true"></div>

<style>
  /* ─── Header shell ───────────────────────────────────────────────────────── */

  .site-header {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 1100;
    background: rgba(10, 10, 10, 0.88);
    backdrop-filter: blur(14px);
    -webkit-backdrop-filter: blur(14px);
    border-bottom: 1px solid var(--color-border-dark);
    transition: background var(--transition-base), border-color var(--transition-base);
  }

  /* Red signal line at the very top */
  .header-accent-line {
    height: 3px;
    background: var(--color-accent-500);
    width: 100%;
  }

  .site-header.scrolled {
    background: rgba(10, 10, 10, 0.97);
    border-color: rgba(255, 255, 255, 0.16);
  }

  /* ─── Nav layout ─────────────────────────────────────────────────────────── */

  .header-nav {
    display: flex;
    align-items: center;
    gap: var(--space-10);
    height: calc(var(--header-height) - 3px);
  }

  /* ─── Logo ───────────────────────────────────────────────────────────────── */

  .header-logo {
    display: flex;
    align-items: center;
    flex-shrink: 0;
    text-decoration: none;
    transition: opacity var(--transition-fast);
  }

  .header-logo:hover { opacity: 0.85; }

  .logo-img {
    height: 46px;
    width: auto;
    display: block;
  }

  @media (max-width: 1023px) {
    .logo-img { height: 38px; }
  }

  /* ─── Desktop nav ────────────────────────────────────────────────────────── */

  .desktop-nav {
    display: none;
    list-style: none;
    margin: 0;
    padding: 0;
    gap: var(--space-2);
    flex: 1;
    height: 100%;
  }

  @media (min-width: 1024px) { .desktop-nav { display: flex; } }

  .nav-item {
    position: relative;
    display: flex;
    align-items: stretch;
  }

  .nav-link {
    display: flex;
    align-items: center;
    gap: var(--space-1);
    padding: 0 var(--space-3);
    font-family: var(--font-display);
    font-size: var(--text-sm);
    font-weight: 500;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: rgba(245, 245, 244, 0.68);
    text-decoration: none;
    transition: color var(--transition-fast);
    position: relative;
    white-space: nowrap;
  }

  .nav-link--btn {
    background: none;
    border: none;
    cursor: pointer;
  }

  /* Red underline pinned to the header's bottom edge */
  .nav-link::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: var(--space-3);
    right: var(--space-3);
    height: 2px;
    background: var(--color-accent-500);
    transform: scaleX(0);
    transform-origin: left;
    transition: transform var(--transition-fast);
  }

  .has-dropdown .nav-link::after { display: none; }

  .nav-link:hover { color: #ffffff; }
  .nav-link:hover::after { transform: scaleX(1); }

  .nav-link--active { color: #ffffff; }
  .nav-link--active::after { transform: scaleX(1); }

  .nav-chevron {
    transition: transform var(--transition-fast);
    flex-shrink: 0;
    opacity: 0.55;
  }

  /* ─── Dropdown panel ─────────────────────────────────────────────────────── */

  .nav-dropdown {
    position: absolute;
    top: 100%;
    left: 0;
    min-width: 360px;
    padding-top: 0;
    background: transparent;
    opacity: 0;
    visibility: hidden;
    transform: translateY(-6px);
    transition:
      opacity var(--transition-fast),
      transform var(--transition-fast),
      visibility var(--transition-fast);
    z-index: 1200;
    pointer-events: none;
  }

  .dropdown-inner {
    background: var(--color-brand-900);
    border: 1px solid rgba(255, 255, 255, 0.12);
    border-top: 2px solid var(--color-accent-500);
    box-shadow: 0 24px 48px rgb(0 0 0 / 0.45);
    padding: var(--space-5);
  }

  .nav-dropdown--areas { min-width: 300px; }

  .nav-item.has-dropdown:hover .nav-dropdown,
  .nav-item.dropdown-open .nav-dropdown {
    opacity: 1;
    visibility: visible;
    transform: translateY(0);
    pointer-events: auto;
  }

  .nav-item.has-dropdown:hover .nav-chevron,
  .nav-item.dropdown-open .nav-chevron {
    transform: rotate(180deg);
    opacity: 1;
  }

  .nav-item.has-dropdown:hover .nav-link,
  .nav-item.dropdown-open .nav-link {
    color: #ffffff;
  }

  .dropdown-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: var(--space-4);
    padding-bottom: var(--space-3);
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
  }

  .dropdown-label {
    font-family: var(--font-display);
    font-size: var(--text-xs);
    font-weight: 600;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: rgba(245, 245, 244, 0.45);
  }

  .dropdown-view-all {
    font-family: var(--font-display);
    font-size: var(--text-xs);
    font-weight: 600;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--color-accent-400);
    text-decoration: none;
    transition: color var(--transition-fast);
  }

  .dropdown-view-all:hover { color: #ffffff; }

  .dropdown-list {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    flex-direction: column;
    gap: 2px;
  }

  .dropdown-list--grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 2px;
  }

  .dropdown-item {
    display: flex;
    align-items: baseline;
    gap: var(--space-3);
    padding: var(--space-2) var(--space-3);
    text-decoration: none;
    transition: background var(--transition-fast);
  }

  .dropdown-item:hover { background: rgba(255, 255, 255, 0.06); }

  .dropdown-item:hover .dropdown-item-name { color: var(--color-accent-400); }

  .dropdown-item-num {
    font-family: var(--font-display);
    font-size: var(--text-xs);
    font-weight: 500;
    letter-spacing: 0.08em;
    color: rgba(245, 245, 244, 0.32);
    font-variant-numeric: tabular-nums;
    flex-shrink: 0;
  }

  .dropdown-item-body {
    display: flex;
    flex-direction: column;
    gap: 2px;
  }

  .dropdown-item--compact {
    align-items: center;
    justify-content: space-between;
    gap: var(--space-2);
  }

  .dropdown-item-name {
    font-family: var(--font-body);
    font-size: var(--text-sm);
    font-weight: 700;
    color: #f5f5f4;
    transition: color var(--transition-fast);
    white-space: nowrap;
  }

  .dropdown-item-desc {
    font-family: var(--font-body);
    font-size: var(--text-xs);
    color: rgba(245, 245, 244, 0.5);
    line-height: 1.45;
  }

  /* ─── Desktop CTA ────────────────────────────────────────────────────────── */

  .header-cta {
    display: none;
    margin-left: auto;
    flex-shrink: 0;
  }

  @media (min-width: 1024px) { .header-cta { display: flex; } }

  .btn-call {
    display: inline-flex;
    align-items: center;
    gap: var(--space-2);
    padding: 0.65rem var(--space-5);
    background: var(--color-accent-500);
    color: #ffffff;
    font-family: var(--font-display);
    font-size: var(--text-sm);
    font-weight: 600;
    letter-spacing: 0.08em;
    text-decoration: none;
    white-space: nowrap;
    font-variant-numeric: tabular-nums;
    transition: background var(--transition-fast);
  }

  .btn-call:hover { background: var(--color-accent-600); }

  /* ─── Mobile toggle ──────────────────────────────────────────────────────── */

  .mobile-menu-toggle {
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: flex-start;
    gap: 5px;
    width: 44px;
    height: 44px;
    background: none;
    border: none;
    cursor: pointer;
    margin-left: auto;
    padding: 10px;
  }

  @media (min-width: 1024px) { .mobile-menu-toggle { display: none; } }

  .toggle-bar {
    display: block;
    height: 2px;
    background: #f5f5f4;
    transition:
      transform 0.3s cubic-bezier(0.4, 0, 0.2, 1),
      opacity 0.2s ease,
      width 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .toggle-bar:nth-child(1),
  .toggle-bar:nth-child(3) { width: 20px; }

  .toggle-bar:nth-child(2) { width: 13px; }

  .mobile-menu-toggle.is-open .toggle-bar:nth-child(1) {
    transform: translateY(7px) rotate(45deg);
    width: 20px;
  }

  .mobile-menu-toggle.is-open .toggle-bar:nth-child(2) {
    opacity: 0;
    width: 0;
  }

  .mobile-menu-toggle.is-open .toggle-bar:nth-child(3) {
    transform: translateY(-7px) rotate(-45deg);
    width: 20px;
  }

  /* ─── Mobile drawer ──────────────────────────────────────────────────────── */

  .mobile-drawer {
    position: fixed;
    top: 0;
    right: 0;
    bottom: 0;
    width: min(340px, 90vw);
    background: var(--color-brand-950);
    z-index: 1200;
    transform: translateX(100%);
    transition: transform 340ms var(--ease-out-strong);
    overflow-y: auto;
    border-left: 1px solid rgba(255, 255, 255, 0.1);
  }

  .mobile-drawer.open { transform: translateX(0); }

  .mobile-drawer-inner {
    padding: var(--space-6);
    display: flex;
    flex-direction: column;
    gap: var(--space-6);
    min-height: 100%;
  }

  .mobile-drawer-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
  }

  .drawer-logo {
    height: 26px;
    width: auto;
    display: block;
  }

  .mobile-drawer-close {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 44px;
    height: 44px;
    background: transparent;
    border: 1px solid rgba(255, 255, 255, 0.25);
    cursor: pointer;
    color: #f5f5f4;
    transition: border-color var(--transition-fast), color var(--transition-fast), background var(--transition-fast);
  }

  .mobile-drawer-close:hover {
    background: var(--color-accent-500);
    border-color: var(--color-accent-500);
    color: #ffffff;
  }

  .mobile-nav-list {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    flex-direction: column;
    border-top: 1px solid rgba(255, 255, 255, 0.1);
  }

  .mobile-nav-link {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: var(--space-4) var(--space-2);
    font-family: var(--font-display);
    font-size: var(--text-lg);
    font-weight: 500;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    color: #f5f5f4;
    text-decoration: none;
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
    transition: color var(--transition-fast), padding-left var(--transition-fast);
  }

  .mobile-nav-link:hover {
    color: var(--color-accent-400);
    padding-left: var(--space-4);
  }

  .mobile-nav-link--active { color: var(--color-accent-400); }

  .mobile-nav-arrow {
    flex-shrink: 0;
    opacity: 0.4;
    transition: opacity var(--transition-fast), transform var(--transition-fast);
  }

  .mobile-nav-link:hover .mobile-nav-arrow,
  .mobile-nav-link--active .mobile-nav-arrow {
    opacity: 1;
    transform: translateX(2px);
  }

  .mobile-nav-section {
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
  }

  .mobile-nav-section .mobile-nav-link {
    border-bottom: none;
    flex: 1;
  }

  .mobile-nav-row {
    display: flex;
    align-items: center;
    gap: var(--space-2);
  }

  .mobile-sub-toggle {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 44px;
    height: 44px;
    flex-shrink: 0;
    background: transparent;
    border: none;
    border-left: 1px solid rgba(255, 255, 255, 0.1);
    color: rgba(245, 245, 244, 0.55);
    cursor: pointer;
    transition: color var(--transition-fast);
  }

  .mobile-sub-toggle:hover { color: #ffffff; }

  .mobile-sub-toggle svg {
    transition: transform var(--transition-fast);
  }

  .mobile-sub-toggle[aria-expanded="true"] {
    color: var(--color-accent-400);
  }

  .mobile-sub-toggle[aria-expanded="true"] svg {
    transform: rotate(180deg);
  }

  /* Collapsible wrapper: closed by default, animates open */
  .mobile-sub-wrap {
    display: grid;
    grid-template-rows: 0fr;
    transition: grid-template-rows 300ms var(--ease-out-strong);
  }

  .mobile-sub-wrap.open {
    grid-template-rows: 1fr;
  }

  .mobile-sub-wrap > .mobile-sub-list {
    overflow: hidden;
    min-height: 0;
  }

  .mobile-sub-list {
    list-style: none;
    margin: 0;
    padding: 0 var(--space-2);
    display: flex;
    flex-direction: column;
    gap: 2px;
    background: rgba(255, 255, 255, 0.03);
  }

  .mobile-sub-wrap.open > .mobile-sub-list {
    padding-bottom: var(--space-3);
  }

  .mobile-sub-link {
    display: block;
    padding: var(--space-2) var(--space-3);
    font-family: var(--font-body);
    font-size: var(--text-sm);
    font-weight: 500;
    color: rgba(245, 245, 244, 0.6);
    text-decoration: none;
    transition: color var(--transition-fast), background var(--transition-fast), padding-left var(--transition-fast);
  }

  .mobile-sub-link:hover {
    color: #ffffff;
    background: rgba(255, 255, 255, 0.05);
    padding-left: var(--space-5);
  }

  .mobile-drawer-cta {
    margin-top: auto;
    display: flex;
    flex-direction: column;
    gap: var(--space-3);
  }

  .drawer-btn-call {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: var(--space-2);
    padding: var(--space-4);
    background: var(--color-accent-500);
    color: #ffffff;
    font-family: var(--font-display);
    font-size: var(--text-base);
    font-weight: 600;
    letter-spacing: 0.08em;
    text-decoration: none;
    transition: background var(--transition-fast);
  }

  .drawer-btn-call:hover { background: var(--color-accent-600); }

  /* ─── Overlay ────────────────────────────────────────────────────────────── */

  .mobile-overlay {
    position: fixed;
    inset: 0;
    background: rgb(0 0 0 / 0.55);
    z-index: 150;
    opacity: 0;
    pointer-events: none;
    transition: opacity 320ms ease;
    backdrop-filter: blur(2px);
  }

  .mobile-overlay.open {
    opacity: 1;
    pointer-events: auto;
  }
</style>

<script>
  // ─── Mobile drawer ──────────────────────────────────────────────────────────
  const toggle   = document.getElementById('mobile-menu-toggle')!;
  const drawer   = document.getElementById('mobile-drawer')!;
  const overlay  = document.getElementById('mobile-overlay')!;
  const closeBtn = document.getElementById('mobile-drawer-close')!;
  const header   = document.getElementById('site-header')!;

  function openDrawer() {
    drawer.classList.add('open');
    overlay.classList.add('open');
    toggle.classList.add('is-open');
    drawer.setAttribute('aria-hidden', 'false');
    toggle.setAttribute('aria-expanded', 'true');
    document.body.style.overflow = 'hidden';
  }

  function closeDrawer() {
    drawer.classList.remove('open');
    overlay.classList.remove('open');
    toggle.classList.remove('is-open');
    drawer.setAttribute('aria-hidden', 'true');
    toggle.setAttribute('aria-expanded', 'false');
    document.body.style.overflow = '';
  }

  toggle.addEventListener('click', openDrawer);
  closeBtn.addEventListener('click', closeDrawer);
  overlay.addEventListener('click', closeDrawer);

  // Expandable drawer sections (Services, Service Areas)
  document.querySelectorAll<HTMLButtonElement>('[data-sub-toggle]').forEach(btn => {
    btn.addEventListener('click', () => {
      const wrap = document.getElementById(btn.getAttribute('aria-controls')!);
      if (!wrap) return;
      const isOpen = wrap.classList.toggle('open');
      btn.setAttribute('aria-expanded', String(isOpen));
    });
  });

  window.addEventListener('scroll', () => {
    header.classList.toggle('scrolled', window.scrollY > 10);
  }, { passive: true });

  document.addEventListener('keydown', (e) => {
    if (e.key === 'Escape') {
      closeDrawer();
      closeAllDropdowns();
    }
  });

  // ─── Desktop dropdowns ──────────────────────────────────────────────────────
  function closeAllDropdowns() {
    document.querySelectorAll('.nav-item.dropdown-open').forEach(item => {
      item.classList.remove('dropdown-open');
      const trigger = item.querySelector('[data-dropdown-trigger]') as HTMLElement | null;
      if (trigger) trigger.setAttribute('aria-expanded', 'false');
    });
  }

  // Toggle on button click (also works for keyboard)
  document.querySelectorAll('[data-dropdown-trigger]').forEach(trigger => {
    trigger.addEventListener('click', (e) => {
      e.stopPropagation();
      const navItem = (trigger as HTMLElement).closest('.nav-item')!;
      const isOpen  = navItem.classList.contains('dropdown-open');

      closeAllDropdowns();

      if (!isOpen) {
        navItem.classList.add('dropdown-open');
        trigger.setAttribute('aria-expanded', 'true');
      }
    });
  });

  // Close when clicking outside
  document.addEventListener('click', (e) => {
    if (!(e.target as HTMLElement).closest('.has-dropdown')) {
      closeAllDropdowns();
    }
  });

  // Keyboard: close on Tab away from the dropdown
  document.querySelectorAll('.nav-dropdown').forEach(dd => {
    dd.addEventListener('keydown', (e) => {
      const ke = e as KeyboardEvent;
      if (ke.key === 'Tab') {
        // small delay to let focus move first
        setTimeout(() => {
          const focused = document.activeElement;
          if (!dd.contains(focused) && !dd.previousElementSibling?.contains(focused)) {
            closeAllDropdowns();
          }
        }, 50);
      }
    });
  });
</script>
